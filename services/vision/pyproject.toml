[project]
name = "decupa-vision"
version = "0.1.0"
description = "Sidecar de índice visual (MediaPipe) do Decupa"
requires-python = ">=3.12,<3.13"
dependencies = [
    "mediapipe>=0.10.21,<1",
]

[tool.uv]
package = false
